<form method="post" action="{{ path('app_link_collection_delete', {'id': link_collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ link_collection.id) }}">
    <button class="btn">Delete</button>
</form>
